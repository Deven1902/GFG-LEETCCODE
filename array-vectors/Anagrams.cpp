class Solution
{
    public:
    //Function is to check whether two strings are anagram of each other or not.
    bool isAnagram(string c, string d){
        
        // Your code here
        // simple solution -> time complexity:- O(N)
        //                    space complexity:- no extra space required. 
                                
        
        sort(a.begin(), a.end());
        sort(b.begin(), b.end());
        
        if(a==b) 
            return true;
            
        else 
            return false;
            
        // optimized solution to be updated. 
    }

};
